<ul>
  <li><%= link_to 'Portada', '/' %>
  <% guides.each do |guide| %>
    <li>
      <%= link_to guide[:title], guide[:path],
                  class: current_page?(guide[:path]) ? 'current' : nil %>
    </li>
  <% end %>
</ul>
